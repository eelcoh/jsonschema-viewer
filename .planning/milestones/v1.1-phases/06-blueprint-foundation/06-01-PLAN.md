---
phase: 06-blueprint-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Render/Theme.elm
  - src/Render/Svg.elm
autonomous: true
requirements:
  - VIS-01
must_haves:
  truths:
    - "All diagram nodes render with outlined style (transparent fill, light border) on dark background"
    - "Dot grid pattern is visible as subtle texture behind diagram content"
    - "Connector lines render in muted blue-gray, visually receding behind node borders"
    - "A Render.Theme module exists exporting all color constants as String values"
  artifacts:
    - path: "src/Render/Theme.elm"
      provides: "Centralized color constants for blueprint theme"
      exports: ["background", "gridDot", "nodeBorder", "nodeFill", "nodeText", "connector", "refBorder", "errorHeading", "errorText", "errorDetailBg"]
    - path: "src/Render/Svg.elm"
      provides: "SVG renderer using Theme colors with background rect and dot grid"
      contains: "import Render.Theme as Theme"
  key_links:
    - from: "src/Render/Svg.elm"
      to: "src/Render/Theme.elm"
      via: "import Render.Theme as Theme"
      pattern: "Theme\\."
---

<objective>
Create the Render.Theme module and transform the SVG renderer from filled-pill-on-white to outlined-nodes-on-dark-navy with a dot grid background.

Purpose: Establishes the blueprint visual foundation (VIS-01) and centralizes all color constants so Phase 7 can modify values in one place.
Output: `src/Render/Theme.elm` (new), `src/Render/Svg.elm` (modified with Theme imports, background, grid)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-blueprint-foundation/06-CONTEXT.md
@.planning/phases/06-blueprint-foundation/06-UI-SPEC.md
@.planning/phases/06-blueprint-foundation/06-RESEARCH.md

@src/Render/Svg.elm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Render.Theme module with all color constants</name>
  <files>src/Render/Theme.elm</files>
  <read_first>
    .planning/phases/06-blueprint-foundation/06-UI-SPEC.md
  </read_first>
  <action>
Create new file `src/Render/Theme.elm` with the following exact content (per D-07, values from 06-UI-SPEC.md Theme Module Contract):

```elm
module Render.Theme exposing
    ( background
    , connector
    , errorDetailBg
    , errorHeading
    , errorText
    , gridDot
    , nodeBorder
    , nodeFill
    , nodeText
    , refBorder
    )


background : String
background =
    "#1a2332"


gridDot : String
gridDot =
    "#2a3a52"


nodeBorder : String
nodeBorder =
    "#a0c4e8"


nodeFill : String
nodeFill =
    "transparent"


nodeText : String
nodeText =
    "#e8f0f8"


connector : String
connector =
    "#4a6a8a"


refBorder : String
refBorder =
    "#a0c4e8"


errorHeading : String
errorHeading =
    "#ff8591"


errorText : String
errorText =
    "#c8d8e8"


errorDetailBg : String
errorDetailBg =
    "#0f1822"
```

This module exports String constants only — no functions, no Color types. Each constant is directly usable as an SVG attribute value. The `src/` directory is already in `elm.json` `source-directories`, so no `elm.json` change is needed.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/jsonschema-viewer && elm make src/Render/Theme.elm --output=/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `src/Render/Theme.elm` exists
    - File contains `module Render.Theme exposing`
    - File contains `background = "#1a2332"`  (per D-01)
    - File contains `gridDot = "#2a3a52"`
    - File contains `nodeBorder = "#a0c4e8"`  (per D-04)
    - File contains `nodeFill = "transparent"` (per D-04)
    - File contains `nodeText = "#e8f0f8"`
    - File contains `connector = "#4a6a8a"`   (per D-06)
    - File contains `refBorder = "#a0c4e8"`   (per D-05)
    - File contains `errorHeading = "#ff8591"` (per D-10)
    - File contains `errorText = "#c8d8e8"`    (per D-10)
    - File contains `errorDetailBg = "#0f1822"` (per D-10)
    - `elm make src/Render/Theme.elm --output=/dev/null` exits 0
  </acceptance_criteria>
  <done>Render.Theme module compiles and exports all 10 color constants as String values</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Render/Svg.elm to Theme colors and add SVG background with dot grid</name>
  <files>src/Render/Svg.elm</files>
  <read_first>
    src/Render/Svg.elm
    src/Render/Theme.elm
    .planning/phases/06-blueprint-foundation/06-UI-SPEC.md
    .planning/phases/06-blueprint-foundation/06-RESEARCH.md
  </read_first>
  <action>
Modify `src/Render/Svg.elm` with three categories of changes:

**A. Add Theme import (per D-08)**

Add after the existing `import Svg.Events` line (around line 12):

```elm
import Render.Theme as Theme
```

**B. Replace all color references (per D-04, D-05, D-06, D-08)**

The complete migration table — every occurrence in the file:

| Line | Current code | Replace with |
|------|-------------|--------------|
| 555 | `darkClr` (in `bg = darkClr \|> SvgA.fill`) | `Theme.nodeFill` |
| 559 | `lightClr` (in `fg = lightClr \|> SvgA.fill`) | `Theme.nodeText` |
| 563 | `lightClr` (in `border = lightClr \|> SvgA.stroke`) | `Theme.nodeBorder` |
| 642 | `darkClr` (in `bg = darkClr \|> SvgA.fill`) | `Theme.nodeFill` |
| 646 | `lightClr` (in `border = lightClr \|> SvgA.stroke`) | `Theme.nodeBorder` |
| 693 | `lightClr` (in `fg = lightClr \|> SvgA.fill`) | `Theme.nodeText` |
| 740 | `lightClr` (in `strokeColor = lightClr \|> SvgA.stroke`) | `Theme.nodeBorder` |
| 812 | `lightClr` (in `fg = lightClr \|> SvgA.fill`) | `Theme.nodeText` |
| 899 | `SvgA.stroke "#8baed6"` | `SvgA.stroke Theme.connector` |

After all replacements, delete the now-unused helper and constants (lines 858-868):
- Delete `color r g b = Color.rgb r g b \|> Color.Convert.colorToHex` (line 858-860)
- Delete `lightClr = "#e6e6e6"` (lines 863-864)
- Delete `darkClr = color 57 114 206` (lines 867-868)

Also remove the now-unused imports `import Color exposing (gray)` and `import Color.Convert` (lines 3-4) since no other code in this file uses them after deleting the `color` helper.

**C. Increase strokeWidth on node rects for outlined style visibility**

The current `SvgA.strokeWidth "0.2"` appears at two locations (inside the `rct` definition in both `viewInlineSchema`/combinator around line 590, and `iconRect` around line 665). Change both from `"0.2"` to `"1"`. This is necessary because switching from filled pills to transparent fill means the border is now the only visible frame around nodes — 0.2px would be nearly invisible (per RESEARCH.md Pitfall 2).

**D. Add SVG background rect and dot grid to `view` function (per D-01, D-02)**

Replace the current `view` function body (lines 36-50):

```elm
view : (String -> msg) -> Set String -> Definitions -> Schema -> Html.Html msg
view toggleMsg collapsedNodes defs schema =
    let
        ( schemaView, ( w, h ) ) =
            viewSchema Set.empty defs collapsedNodes toggleMsg "root" ( 0, 0 ) Nothing "700" schema

        vb =
            viewBoxString w h 20
    in
    Svg.svg
        [ SvgA.width "100%"
        , SvgA.height "100%"
        , SvgA.viewBox vb
        ]
        [ Svg.defs []
            [ Svg.pattern
                [ SvgA.id "dot-grid"
                , SvgA.x "0"
                , SvgA.y "0"
                , SvgA.width "20"
                , SvgA.height "20"
                , SvgA.patternUnits "userSpaceOnUse"
                ]
                [ Svg.circle
                    [ SvgA.cx "10"
                    , SvgA.cy "10"
                    , SvgA.r "0.5"
                    , SvgA.fill Theme.gridDot
                    ]
                    []
                ]
            ]
        , Svg.rect
            [ SvgA.width "100%"
            , SvgA.height "100%"
            , SvgA.fill Theme.background
            ]
            []
        , Svg.rect
            [ SvgA.width "100%"
            , SvgA.height "100%"
            , SvgA.fill "url(#dot-grid)"
            ]
            []
        , schemaView
        ]
```

The `<defs>` block defines the repeating dot grid pattern (20x20 userSpaceOnUse tiles with a 0.5px radius circle). The first `<rect>` is the dark navy background fill. The second `<rect>` applies the dot grid pattern on top. `schemaView` renders last, on top of both.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/jsonschema-viewer && elm make src/Main.elm --output=/dev/null && grep -c "darkClr\|lightClr\|#8baed6\|import Color" src/Render/Svg.elm | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Render/Svg.elm` contains `import Render.Theme as Theme`
    - `src/Render/Svg.elm` does NOT contain the string `darkClr` anywhere
    - `src/Render/Svg.elm` does NOT contain the string `lightClr` anywhere
    - `src/Render/Svg.elm` does NOT contain the string `#8baed6` anywhere
    - `src/Render/Svg.elm` does NOT contain `import Color`
    - `src/Render/Svg.elm` contains `Theme.nodeFill` (at least 2 occurrences)
    - `src/Render/Svg.elm` contains `Theme.nodeText` (at least 3 occurrences)
    - `src/Render/Svg.elm` contains `Theme.nodeBorder` (at least 3 occurrences)
    - `src/Render/Svg.elm` contains `Theme.connector` (at least 1 occurrence)
    - `src/Render/Svg.elm` contains `SvgA.id "dot-grid"` (SVG pattern definition)
    - `src/Render/Svg.elm` contains `Theme.background` (background rect fill)
    - `src/Render/Svg.elm` contains `Theme.gridDot` (grid dot fill)
    - `src/Render/Svg.elm` contains `SvgA.patternUnits "userSpaceOnUse"` (correct pattern units)
    - `src/Render/Svg.elm` contains `SvgA.strokeWidth "1"` (visible outlined borders, not "0.2")
    - `src/Render/Svg.elm` contains `SvgA.strokeDasharray "5 3"` (dashed $ref border preserved, per D-05)
    - `elm make src/Main.elm --output=/dev/null` exits 0
  </acceptance_criteria>
  <done>All color references migrated to Theme module, old constants deleted, SVG background and dot grid added, node borders increased to visible width, full app compiles</done>
</task>

<task type="auto">
  <name>Task 3: Update CSS for dark diagram panel and error display</name>
  <files>src/main.css</files>
  <read_first>
    src/main.css
    .planning/phases/06-blueprint-foundation/06-UI-SPEC.md
  </read_first>
  <action>
Modify `src/main.css` with exactly these CSS property changes (per D-03, D-09, D-10, values from 06-UI-SPEC.md CSS Changes table):

**1. `.diagram-panel` (around line 139):**
Change `background: #ffffff;` to `background: #1a2332;`

This is the CSS fallback per D-03 — prevents white flash before the Elm SVG renders.

**2. `.error-heading` (around line 157):**
Change `color: #cf222e;` to `color: #ff8591;`

**3. `.error-body` (around line 165):**
Change `color: #293c4b;` to `color: #c8d8e8;`

**4. `.error-detail` (around lines 173, 175):**
Change `color: #293c4b;` to `color: #c8d8e8;`
Change `background: #f6f8fa;` to `background: #0f1822;`

No other CSS selectors are changed. Input panel and toolbar remain light per D-09.

After changes, copy CSS to public directory:
```bash
cp src/main.css public/main.css
```
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/jsonschema-viewer && grep "background: #1a2332" src/main.css && grep "color: #ff8591" src/main.css && grep "color: #c8d8e8" src/main.css && grep "background: #0f1822" src/main.css && ! grep "#ffffff" src/main.css | grep -q "diagram-panel"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.css` `.diagram-panel` contains `background: #1a2332;` (not #ffffff)
    - `src/main.css` `.error-heading` contains `color: #ff8591;` (not #cf222e)
    - `src/main.css` `.error-body` contains `color: #c8d8e8;` (not #293c4b)
    - `src/main.css` `.error-detail` contains `color: #c8d8e8;` and `background: #0f1822;`
    - `src/main.css` still contains `#f6f8fa` in `.input-panel` (unchanged per D-09)
    - `public/main.css` is a copy of `src/main.css`
  </acceptance_criteria>
  <done>CSS updated for dark diagram panel background and dark-adapted error display colors, input panel unchanged</done>
</task>

</tasks>

<verification>
1. Full compile: `elm make src/Main.elm --output=/dev/null` exits 0
2. Test suite: `elm-test` passes (no regressions)
3. No old colors: `grep -c "darkClr\|lightClr\|#8baed6\|import Color" src/Render/Svg.elm` returns 0
4. Theme exports: `grep -c "^[a-z].*: String$" src/Render/Theme.elm` returns 10
5. Build for visual check: `elm make src/Main.elm --output=public/elm.js --optimize && cp src/main.css public/main.css`
6. Visual inspection: Open `public/index.html` — diagram renders on dark navy background with dot grid, outlined nodes with light borders and white text, muted blue-gray connectors
</verification>

<success_criteria>
- Render.Theme module exists with 10 color constants (VIS-01)
- All diagram colors flow through Theme module (D-07, D-08)
- Dark navy background with dot grid visible in SVG (D-01, D-02)
- Nodes display as outlined shapes on dark canvas (D-04)
- $ref nodes retain dashed border (D-05)
- Connector lines are muted blue-gray (D-06)
- CSS diagram panel background is #1a2332 (D-03)
- Error display adapted for dark background (D-10)
- Input panel and toolbar unchanged (D-09)
- Full app compiles and all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-blueprint-foundation/06-01-SUMMARY.md`
</output>
